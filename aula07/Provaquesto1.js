let notasAluno = [1 + 4 + 3]

let media = notasAluno / 3

let mediaformatada = media.toFixed(1)

if (mediaformatada >= 7) {
    alert("Parábens você passou de ano com uma media de " + mediaformatada);
}
else {
    alert("você reprovou de ano, com a incrivel media de " + mediaformatada, "🤣");
}